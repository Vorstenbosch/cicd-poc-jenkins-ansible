# CICD poc
This is a poc showcasing jenkins, jenkins agents and ansible to build a CI/CD pipeline and manage a complex environment. It will be ran locally using vagrant boxes.

## Prepare host
```
# for windows
choco install virtualbox vagrant
```